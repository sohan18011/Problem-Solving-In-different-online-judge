#include<bits/stdc++.h>
using namespace std;
int main()
{
    vector<vector<int>>vt;
    int n;
    cin>>n;
    a[n+2];
    int i,j,k;
    for(i=0;i<n;i++)cin>>a[i];
    sort(a,a+n);

    for(i=0-;i<n;i++)
    {
        for(i=0;i<n;i++)
        {
            int sum2=a[i]+a[j];
            int num3=0-sum2;
            int num=find(num3)
        }
    }
}
